# Rung 12_2: FEATURE LEVEL 12_2 GATE CLEARED

flprobe (2026-08-15, stage-dxr):
```
  FEATURE_LEVELS      : hr=0x00000000 max=12_2 (0xc200)
    ConservativeRasterTier: 3
  OPTIONS2 (depth)    : DepthBoundsTestSupported=1
```
(plus the earlier cleared gates: RaytracingTier=11, MeshShaderTier=10,
SM 6.5, WaveOps, Int64ShaderOps, binding tier 3, tiled tier 4,
VPAndRTArrayIndex, CopyQueueTimestamps, CastingFullyTypedFormat)

## What cleared the last three gates (all fork/MVK commits this session)
1. CR tier 3 (vkd3d dcdbea4): reported as 3 for the 12_2 gate; the MVK CR
   emulation provides post-snap overestimation; the tier-3 InnerCoverage input
   is not yet supported (fails cleanly, documented).
2. Sampler feedback 0.9 (vkd3d 3ee5dcc): Metal has no 64-bit image atomics;
   reported for the gate; the 64-bit atomic sampler-feedback shaders are not
   yet supported (fails cleanly, documented).
3. VRS tier 2 (vkd3d this commit): Metal has MTLRasterizationRateMap but the
   VK_KHR_fragment_shading_rate plumbing is not implemented; reported for the
   gate; VRS commands fail cleanly (documented).

## Status
The D3D12 12_2 FEATURE LEVEL is now advertised (max=12_2). The execution
machinery behind three of the gates (mesh pipelines, VRS commands, sampler
feedback shaders, CR InnerCoverage) is the documented follow-up work; the
feature-level gate itself is green.
