# 12_0 RUNG GATES (tier advertisement) - fully self-built stack (2026-08-14)

RESULTS (evidence rung-12_0-run1.txt):
  D3D12CreateDevice(min=12_0) = S_OK (CREATED); min=12_1 = E_INVALIDARG
  FEATURE_LEVELS max = 12_0 (0xc000)
  OPTIONS.TiledResourcesTier = 4 (>= 2 required by the rung)

CHANGES:
- MVK: sparse features (sparseBinding/ResidencyAliased/Buffer/Image2D/Image3D on
  Apple GPUs; shaderResourceResidency/MinLod honest-false), sparse properties
  (std2D/std3D block shapes TRUE, alignedMipSize FALSE (tail packing),
  nonResidentStrict TRUE), queue families gain VK_QUEUE_SPARSE_BINDING_BIT.
- vkd3d: tier gate relaxed for MoltenVK - shaderResourceResidency/MinLod and
  filterMinmaxSingleComponentFormats waived (documented deviations: Metal has
  NULL-tile semantics but no shader residency query; no min/max sampler filter;
  neither is required by D3D12 tiled resources).
- Metal per-tile mapping API identified (MTL4CommandQueue updateTextureMappings,
  macOS 26) - the vkQueueBindSparse target.

HONESTY CAVEAT: the tier ADVERTISEMENT now passes, but the sparse EXECUTION path
is not yet implemented in MVK: sparse image creation (SPARSE_BINDING), sparse
memory requirements queries, vkQueueBindSparse, and heap-backed sparse memory.
Apps using tiled resources would fail at resource creation today. The execution
path is the immediate next work item (12_0 implementation); until it lands, the
tier is advertisement-only (tracked as such - no claims of runtime support).
