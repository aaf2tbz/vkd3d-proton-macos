# 11_1 RUNG GATES — fully self-built stack (2026-08-14)

FULLY SELF-BUILT: our vkd3d-proton (upstream + 5 ported patches) + our MoltenVK
(upstream + logic-op emulation + robustness + denorm + sparse-limits patches) +
DXVK dxgi + MetalSharp Wine 11.5 (with our MVK injected via DYLD override of the
runtime's fused libMoltenVK.1.dylib).

RESULTS (evidence rung-11_1-run10.txt, UUID F9A1E964 = our MVK loaded):
  D3D12CreateDevice(min=11_1) = S_OK (CREATED); min=12_0 = E_INVALIDARG
  FEATURE_LEVELS max = 11_1 (0xb100)
  OPTIONS.OutputMergerLogicOp = 1   <- logic-op emulation live in D3D12
  SHADER_MODEL (mingw id 7) = 0x65 (6.5)

KEY DISCOVERY: the runtime's winevulkan loads a FUSED MoltenVK
($R/lib/wine/x86_64-unix/libMoltenVK.1.dylib), ignoring VK_ICD_FILENAMES.
Our MVK is injected by placing it under that name on DYLD_LIBRARY_PATH.
Additionally: VK_SHADER_FLOAT_CONTROLS_INDEPENDENCE_32_BIT_ONLY=0, NONE=2
(enum inverted vs common assumption) - the denorm relaxation now fires regardless.

logic-op logic-op emulation: 16/16 ops pixel-exact on GPU (vk-logicop probe, native).
